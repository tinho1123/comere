name: Deploy to Hostinger

on:
  push:
    branches:
      - main

jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup PHP
        uses: shivammathur/setup-php@v2
        with:
          php-version: '8.4'

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '22'
          cache: 'npm'

      - name: Install Node dependencies
        run: npm ci

      - name: Build assets
        run: npm run build

      - name: Install PHP dependencies
        run: composer update --no-dev --optimize-autoloader --no-interaction

      - name: Upload projeto para Hostinger
        uses: appleboy/scp-action@v0.1.7
        with:
          host: ${{ secrets.SSH_HOST }}
          port: ${{ secrets.SSH_PORT }}
          username: ${{ secrets.SSH_USER }}
          password: ${{ secrets.SSH_PASSWORD }}
          source: "app/,bootstrap/,config/,database/,lang/,public/,resources/views/,routes/,vendor/,artisan,composer.json,composer.lock"
          target: "/home/u968588512/domains/comere.com.br/comere/"
          timeout: 60s
          command_timeout: 30m

      - name: Run post-deploy commands
        uses: appleboy/ssh-action@v1.0.3
        with:
          host: ${{ secrets.SSH_HOST }}
          port: ${{ secrets.SSH_PORT }}
          username: ${{ secrets.SSH_USER }}
          password: ${{ secrets.SSH_PASSWORD }}
          script: |
            cd ~/domains/comere.com.br/comere

            PHP=/opt/alt/php84/usr/bin/php

            echo "→ Criando estrutura de diretórios..."
            mkdir -p public/storage/store storage/framework/cache storage/framework/sessions storage/framework/views storage/logs

            echo "→ Executando migrations..."
            $PHP artisan migrate --force

            echo "→ Otimizando aplicação..."
            $PHP artisan config:cache
            $PHP artisan route:cache
            $PHP artisan view:cache
            $PHP artisan event:cache
            $PHP artisan icons:cache
            $PHP artisan filament:cache-components
            $PHP artisan optimize

            echo "→ Linkando storage público..."
            $PHP artisan storage:link --force || true